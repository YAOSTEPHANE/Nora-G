services:
  mongo:
    image: mongo:7
    container_name: nora-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - nora-mongo-data:/data/db

volumes:
  nora-mongo-data:
